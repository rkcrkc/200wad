/**
 * Single source of truth for the legal/company details used across the
 * Privacy Policy, Terms of Service and Refunds pages.
 *
 * ⚠️ BEFORE PUBLISHING: replace every [BRACKETED PLACEHOLDER] below with the
 * real registered details. They are intentionally obvious so nothing ships
 * half-filled. These pages are a good-faith template, not legal advice — have
 * them reviewed by a qualified adviser (esp. HK PDPO + UK/EU GDPR + consumer
 * law) before you rely on them.
 */

/** Registered/trading name of the operating entity. */
export const LEGAL_ENTITY = "[LEGAL ENTITY NAME]";

/** The public product/brand name. */
export const BRAND_NAME = "200 Words a Day";

/** Full registered address (Hong Kong). */
export const LEGAL_ADDRESS = "[REGISTERED ADDRESS, Hong Kong]";

/** Company/business registration number, if you want to show it. */
export const COMPANY_NUMBER = "[BUSINESS REGISTRATION NO.]";

/** Inbox for privacy / data-protection requests. */
export const PRIVACY_EMAIL = "[privacy@yourdomain.com]";

/** General support / billing inbox. */
export const SUPPORT_EMAIL = "[support@yourdomain.com]";

/**
 * UK/EU (Art. 27) representative, if appointed. Because the service is offered
 * to UK/EEA users from outside those regions, a representative may be required.
 */
export const UK_EU_REPRESENTATIVE = "[UK/EU representative — name & contact, if appointed]";

/** Governing-law jurisdiction for the Terms. */
export const GOVERNING_LAW = "Hong Kong";

/**
 * Human-readable "last updated" date shown on every legal page. Bump this
 * whenever the substance of any policy changes.
 */
export const LEGAL_LAST_UPDATED = "9 July 2026";

/** How long backups may retain data after account deletion. */
export const BACKUP_RETENTION = "[e.g. 30–90 days]";
